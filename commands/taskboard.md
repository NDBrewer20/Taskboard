---
description: Show what is on the Taskboard right now
---

Call `taskboard_board` and summarise what came back for the user: the columns, how many
tasks sit in each, which are done, and anything whose checklist is part finished. Keep it
short - a few lines per column, not a dump of the json.

$ARGUMENTS
